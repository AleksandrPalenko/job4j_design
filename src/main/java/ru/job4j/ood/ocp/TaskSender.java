package ru.job4j.ood.ocp;

public class TaskSender {
    /*
    public void send(String text, MemTask place ) {
        if (place == MemTask.GIT) {
            sendGIT(text);
        } else if (place == MemTask.SITE) {
            sendSITE(text);
        }
    }
     Здесь идет нарушение принципа OCP, так как если нам нужно будет добавить новый функционал -
     отправление задания через телеграмм, то нужно менять весь класс, а вместо этого, если сделать один интерфейс
     с методом send(), чтобы к нему реализовать любые стратегии.
     */

}
